# Retired Feedback Page

This archive keeps the former user-facing `Rate & Report` page.

It was removed from the active product because the app is now focused on card and map based housing search. The feedback submission API and admin feedback view remain outside this archive so historical feedback can still be reviewed.

Archived files:

- `frontend/app/feedback/page.tsx`
- `frontend/components/FeedbackForm.tsx`
