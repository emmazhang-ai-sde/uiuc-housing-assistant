# ── LangChain ecosystem (versions from working local venv) ──────────────────
langchain==1.3.4
langchain-core==1.4.1
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langchain-chroma==1.1.0
langchain-huggingface==1.2.2
langchain-groq                     # cloud LLM (Groq free tier; replaces NVIDIA)
langchain-nvidia-ai-endpoints     # kept for reference; replaced by Groq

# ── Vector store & embeddings ───────────────────────────────────────────────
chromadb==1.5.9 
sentence-transformers==5.5.1
transformers==5.10.2
torch==2.12.0                     # required by sentence-transformers

# ── API server (Phase 3 Railway backend) ────────────────────────────────────
fastapi==0.136.3
uvicorn==0.49.0
pydantic==2.13.4
PyJWT==2.9.0

# ── Shared utilities ────────────────────────────────────────────────────────
python-dotenv==1.2.2
huggingface_hub==1.18.0

# ── Local dev only (not used on Railway) ────────────────────────────────────
langchain-ollama==1.1.0 # for local dev LLM; not used on Railway
ollama==0.6.2           # for local dev LLM; not used on Railway
streamlit==1.58.0       # for local dev UI; not used on Railway
pandas==3.0.3           # for local dev data manipulation; not used on Railway
playwright==1.60.0      # for web scraping in local dev; not used on Railway
