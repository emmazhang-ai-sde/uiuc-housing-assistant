# Core RAG pipeline
langchain==1.3.4
langchain-core==1.4.1
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langchain-chroma==0.2.4
langchain-huggingface==0.2.0
chromadb==1.5.9
sentence-transformers==5.5.1

# LLM — Phase 2 (local), Phase 3 (cloud)
langchain-ollama==1.1.0       # Phase 2 local only; not needed on Railway
langchain-nvidia-ai-endpoints # Phase 3 cloud deployment

# API server (Phase 3)
fastapi
uvicorn

# Frontend (Phase 2 Streamlit)
streamlit==1.58.0
pandas==3.0.3

# Data and environment
python-dotenv==1.2.2

# Scraper (only needed to re-scrape; not required to run the app)
playwright==1.60.0
