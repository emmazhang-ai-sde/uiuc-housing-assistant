# Core RAG pipeline
langchain==1.3.4
langchain-core==1.4.1
langchain-community==0.4.2
langchain-ollama==1.1.0
langchain-text-splitters==1.1.2
langchain-chroma==0.2.4       # replaces langchain-community Chroma — compatible with chromadb 1.x
langchain-huggingface==0.2.0  # replaces langchain-community HuggingFaceEmbeddings
chromadb==1.5.9
sentence-transformers==5.5.1

# LLM runtime (local)
# For Phase 3 deployment, replace langchain-ollama with langchain-openai
# and set OPENAI_API_KEY as an environment variable
ollama==0.6.2

# Frontend
streamlit==1.58.0
pandas==3.0.3

# Data and environment
python-dotenv==1.2.2

# Scraper (only needed to re-scrape; not required to run the app)
playwright==1.60.0
